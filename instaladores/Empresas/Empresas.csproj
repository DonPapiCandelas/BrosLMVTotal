<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <UseWPF>true</UseWPF>
    <AssemblyName>BrosLMV-Instalador</AssemblyName>
    <RootNamespace>BrosLMV.Empresas</RootNamespace>
    <ApplicationIcon>app.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <LangVersion>latest</LangVersion>
    <AssemblyTitle>BrosLMV - Instalador</AssemblyTitle>
    <Product>BrosLMV</Product>
    <!-- Valor de respaldo si se compila fuera de build\generar_exes.ps1 (que pasa
         /p:Version= tomado del addon empacado, fuente de verdad real). -->
    <Version>2.18.1</Version>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="System.Data" />
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.IO.Compression.FileSystem" />
  </ItemGroup>

  <ItemGroup>
    <Resource Include="assets\logo_blanco.png" />
    <Resource Include="assets\logo_mono.png" />
    <Resource Include="assets\app_icon.png" />
    <EmbeddedResource Include="assets\provision_empresa.sql" />
    <EmbeddedResource Include="payload.zip" />
  </ItemGroup>

  <ItemGroup>
    <!-- Mismo hash de contrasena que verifica el addon (src\ConsolaPasswordHash.cs), enlazado
         (no copiado) para que instalador y addon nunca se desincronicen. -->
    <Compile Include="..\..\src\ConsolaPasswordHash.cs" Link="ConsolaPasswordHash.cs" />
  </ItemGroup>
</Project>
