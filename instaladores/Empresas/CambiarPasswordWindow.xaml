<Window x:Class="BrosLMV.Empresas.CambiarPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cambiar contraseña de la Consola" Height="380" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F4F6F9" FontFamily="Segoe UI">
  <Window.Resources>
    <Style TargetType="Button" x:Key="Primary">
      <Setter Property="Background" Value="#2D6FE0"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="16,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border CornerRadius="6" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Button" x:Key="Ghost">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="#2D6FE0"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="14,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="#CBD5E1" BorderThickness="1" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" x:Name="lblEmpresa" Text="Empresa: —" FontSize="16" FontWeight="Bold" Foreground="#16263A"/>
    <TextBlock Grid.Row="1" Text="Escribe la contraseña actual y la nueva (dos veces, para confirmarla)." Foreground="#64748B" Margin="0,6,0,16" TextWrapping="Wrap"/>

    <StackPanel Grid.Row="2" Margin="0,0,0,10">
      <TextBlock Text="Contraseña actual" Foreground="#64748B" FontSize="11"/>
      <PasswordBox x:Name="pwdActual" Height="32" Padding="6,5" VerticalContentAlignment="Center" Margin="0,3,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Margin="0,0,0,10">
      <TextBlock Text="Contraseña nueva" Foreground="#64748B" FontSize="11"/>
      <PasswordBox x:Name="pwdNueva" Height="32" Padding="6,5" VerticalContentAlignment="Center" Margin="0,3,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="4" Margin="0,0,0,10">
      <TextBlock Text="Confirmar contraseña nueva" Foreground="#64748B" FontSize="11"/>
      <PasswordBox x:Name="pwdConfirmar" Height="32" Padding="6,5" VerticalContentAlignment="Center" Margin="0,3,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancelar" Style="{StaticResource Ghost}" Margin="0,0,10,0" Click="Cancelar_Click"/>
      <Button Content="Cambiar contraseña" Style="{StaticResource Primary}" Click="Cambiar_Click"/>
    </StackPanel>
  </Grid>
</Window>
