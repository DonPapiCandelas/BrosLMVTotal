<Window x:Class="BrosLMV.DescargasUI.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BrosLMV Descargas" Height="190" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Contraseña de la FIEL" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="No se guarda en disco -- solo vive en memoria mientras el programa esta abierto."
                   Foreground="#64748b" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <PasswordBox Grid.Row="2" Name="PwdBox" Padding="6" KeyDown="PwdBox_KeyDown"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Entrar" Width="90" Click="Entrar_Click" Background="#2563eb" Foreground="White" Padding="6"/>
        </StackPanel>
    </Grid>
</Window>
